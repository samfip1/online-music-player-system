services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: sonicvote
      POSTGRES_PASSWORD: sonicvote
      POSTGRES_DB: sonicvote
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
